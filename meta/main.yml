---
galaxy_info:
  author: Kyle Evers
  description: Install Guacamole
  company: CISA Cyber Assessments
  license: CC0
  min_ansible_version: 2.0
  platforms:
    - name: Debian
      versions:
        - stretch
        - buster
        # Kali linux isn't an option here, but it is based on
        # Debian Testing:
        # https://www.kali.org/docs/policy/kali-linux-relationship-with-debian
        - bullseye
    - name: Ubuntu
      versions:
        # The apache2.service file does not appear to be present when
        # apache is installed on xenial.  I'm not sure what is going
        # on there, but until that changes we can't support xenial.
        # - xenial
        - bionic
  galaxy_tags:
    - guacamole

dependencies:
  - src: https://github.com/cisagov/ansible-role-docker
    name: docker
  - src: https://github.com/cisagov/ansible-role-httpd
    name: httpd
